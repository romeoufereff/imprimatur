---
name: design-system
description: |
  Minimalist Slide Deck System (Outline) — the brand knowledge base for this pack. Token names and values,
  gradients, the type scale, and the template library. Read this whenever you need a
  concrete brand value; read the engine's references/design-craft.md for how to use it.
---

# Minimalist Slide Deck System (Outline) (knowledge base)

Generated by design-system-forge from this pack's manifest. Everything below is
derived from `design-system.json` and `tailwind.config.js`, so it cannot drift from
the tokens the validator enforces — regenerate the pack and this file follows.

## Stack

- Canvas **1280×720**, scaled to fit by the scaler in `slide-base.html`
- Tailwind Play CDN with an inline config (a snapshot of `tailwind.config.js`)
- Token prefix `out-` · font utility `font-out`
- Typeface: Archivo

## Tokens

| token | hex | role |
|---|---|---|
| `out-primary` | `#201e1d` | primary |
| `out-primary-mid` | `#605d5d` | primary-mid |
| `out-accent` | `#ae1800` | accent |
| `out-deep` | `#2d2b2b` | deep |
| `out-support-1` | `#7d7979` | support-1 |
| `out-support-2` | `#9b9797` | support-2 |
| `out-ink` | `#201e1d` | ink |
| `out-body` | `#444141` | body |
| `out-muted` | `#605d5d` | muted |
| `out-muted-soft` | `#7d7979` | muted-soft |
| `out-rule` | `#d7d3d3` | rule, rule-neutral |
| `out-tint` | `#f8f4f4` | tint |
| `out-surface` | `#eae9e9` | surface |
| `out-white` | `#ffffff` | white |
| `out-neutral-400` | `#bab6b6` | — |
| `out-accent-600` | `#dd2b0f` | — |

Chart series order: `out-body` → `out-support-1` → `out-neutral-400` → `out-accent`. Adjacent series must stay distinguishable; do not reorder casually.

## Type scale

| step | size | line-height | weight |
|---|---|---|---|
| `text-out-display` | 72px | 1.1 | 700 |
| `text-out-section` | 64px | 1.15 | 700 |
| `text-out-title` | 56px | 1.15 | 700 |
| `text-out-h2` | 40px | 1.2 | 700 |
| `text-out-h3` | 24px | 1.3 | 700 |
| `text-out-metric` | 56px | 1.0 | 700 |
| `text-out-lead` | 20px | 1.6 | 400 |
| `text-out-subhead` | 24px | 1.3 | 700 |
| `text-out-body-sm` | 15px | 1.55 | 400 |
| `text-out-label` | 12px | 1.4 | 400 |
| `text-out-eyebrow` | 12px | 1.4 | 400 |
| `text-out-caption` | 14px | 1.4 | 400 |

Nothing below **12px**. Weights are limited to 400, 700 — anything else fails the audit.

## Template library

Copy a template verbatim to start a slide — the `data-template` attribute is how the validator knows a slide came from the pack rather than from memory.

| file | what it is for |
|---|---|
| `templates/01-cover.html` | Opening slide: eyebrow, headline, one supporting line. |
| `templates/02-section.html` | Divider that marks a turn in the argument. |
| `templates/03-two-column.html` | Two things compared side by side. |
| `templates/04-statement.html` | One idea, large, with room around it. |
| `templates/05-metrics.html` | Three measures as equal cards. |
| `templates/06-closing.html` | Next steps and the ask. |

These are a floor, not a ceiling: they are generated from this pack's tokens so they are correct and on-brand, not so they are the best possible slide. A brief that deserves a bespoke layout should get one.

## Where the craft rules live

Layout, typography practice, SVG and chart rules are brand-independent and live once in the engine, at `references/design-craft.md`. This file is only the brand half.

