# Designer eval 1 — two-column risks/mitigations (sparse)

Use `deck-brief.md` in this folder as the deck's brief. Generate exactly one slide.

```
SLIDE 8: Every delivery risk has a named owner and a mitigation already running
─────────────────────────────────────────────────────────────────────────────
Message:    "The three delivery risks are known, owned, and actively mitigated"
Structure:  Two columns — Risks (left) vs. Mitigations (right), paired rows
Visual:     none (the pairing carries the slide)
Key data:   - Risk: legacy-DWH knowledge concentrated in 2 FTEs → Mitigation: paired
              rotation + runbook sprint (owner: E. Larsen, running since May)
            - Risk: nightly-batch cutover window is 4h → Mitigation: dual-run pilot on
              order-tracking domain first (owner: platform team)
            - Risk: data-governance sign-off pending → Mitigation: DPO workshop booked,
              decision date 15 Aug (owner: sponsor)
Emphasis:   The 1:1 pairing — every risk row visibly matched by a mitigation row
Audience:   executive
Density:    3 risk rows + 3 mitigation rows + 1 callout = 7 items (sparse budget 8)
```
